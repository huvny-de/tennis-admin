export default class Yard {
  constructor(imgUrl, name, type, width, height) {
    this.imgUrl = imgUrl;
    this.name = name;
    this.type = type;
    this.width = width;
    this.height = height;
  }
}
